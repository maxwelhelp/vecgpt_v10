[project]
name = "vecgpt"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["torch", "pillow", "scipy"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["vecgpt*"]
